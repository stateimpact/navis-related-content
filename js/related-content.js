(function($) {
    // function to handle tinymce click
    
    // callback to load recent posts and post-types
    function fetch(args) {
        var data = {
            action: 'fetch_related_content'
        }
    }
    // callback to save module data
    
})(window.jQuery);